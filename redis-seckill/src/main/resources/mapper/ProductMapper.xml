<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yiban.redisseckill.mapper.ProductMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO product (name, price, stock, description)
        VALUES (#{name}, #{price}, #{stock}, #{description})
    </insert>

    <select id="selectById" resultType="com.yiban.redisseckill.bean.Product">
        SELECT * FROM product WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE product
        SET name = #{name}, price = #{price}, stock = #{stock}, description = #{description}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM product WHERE id = #{id}
    </delete>
</mapper>